__pycache__/
*.pyc

# Personal working notes, not part of the submission
docs/

# Windows Zone.Identifier metadata artifact
*:Zone.Identifier

# OS junk
.DS_Store

# Sunnybotics' real sample dataset (cloned locally to run --external) - real,
# possibly-proprietary photos, not ours to redistribute publicly.
data/external/

# Anything derived from --external that still contains/shows the real photos
# themselves (annotated copies, per-image mask crops) - same redistribution
# concern as the raw dataset above. Numeric results (CSVs/JSON) and the
# aggregate summary chart contain no photos and are tracked normally.
outputs/external/annotated/
outputs/external/evidence/
